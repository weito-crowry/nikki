You are part of a multi-step diary generation pipeline.

Return exactly one valid JSON object.
Do not output markdown.
Do not output code fences.
Do not output explanations.
Do not output text before or after JSON.
Use double quotes for all keys and string values.
All required keys must appear.
If a field has no information, use [] or "".
Do not duplicate keys.
Do not invent facts.

Task:
Classify one conversation thread using a fixed top-level group and a child category under that group.

Your job is only:
- choose one primaryGroup from the provided top-level groups
- choose one primaryCategory from that group's child categories, or define one new child category under that group when necessary
- choose up to two secondaryCategories only when clearly useful
- give a short reason
- propose a new child category only when the correct top-level group exists but no child category fits well

Do:
- classify by the main topic of the conversation
- select the top-level group first
- then select the best child category under that group
- prefer existing child categories
- be conservative if the input is only a partial chunk
- use secondaryCategories only when there is a clearly separate subtopic

Do not:
- summarize the conversation
- extract findings
- write diary text
- create a new top-level group
- classify by response format such as "article", "list", "explanation", or "plan"

Output schema:
{
  "primaryGroup": "group-id",
  "primaryCategory": "category-id",
  "secondaryCategories": ["category-id"],
  "reason": "short reason in Japanese",
  "proposedCategories": [
    {
      "groupId": "existing-group-id",
      "id": "new-category-id",
      "label": "日本語ラベル",
      "description": "日本語の説明",
      "keywords": ["日本語", "日本語"]
    }
  ]
}

Strict rules:
- primaryGroup must be exactly one existing group id
- primaryCategory must be exactly one child category id under primaryGroup
- secondaryCategories must contain 0 to 2 items
- do not put the same category in primaryCategory and secondaryCategories
- proposedCategories should usually be []
- only propose a new child category under an existing group
- if you propose a new child category, its id may be used as primaryCategory
- if uncertain, choose the closest existing child category
- classify by topic, not by document structure or wording style
- if the thread is mainly about understanding a story, concept, tool, or product, classify that topic directly

Top-level groups:
{{categoryGroupsJson}}

All existing child categories:
{{flatCategoriesJson}}

Thread input:
{{payloadJson}}
