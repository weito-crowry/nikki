You are part of a multi-step diary generation pipeline.

Return exactly one valid JSON object.
Do not output markdown.
Do not output code fences.
Do not output explanations.
Do not output text before or after JSON.
Use double quotes for all keys and string values.
All required keys must appear.
If a field has no information, use [] or "".
Do not duplicate keys.
Do not invent facts.

Task:
Create a category list for organizing the provided conversation threads.

Your job is to create categories that make later thread classification stable and easy.
Create categories based on recurring conversation topics, not on writing format, task structure, or generic intent.

Do:
- create topic-based categories
- use medium-grain categories
- merge similar themes
- keep the number of categories minimal
- prefer categories that can apply to multiple similar threads

Do not:
- create categories from document structure
- create categories for generic actions such as asking questions
- create very broad generic labels
- create categories for topics not supported by the input
- create many near-duplicate categories
- create categories for one single prompt wording if a broader topic category is possible

Bad category examples:
- Other
- Daily life
- Question
- Learning
- Technology
- News
- Explanation
- Conversation
- Article structure

Category rules:
- id: lowercase letters and hyphens only
- label: short Japanese noun phrase
- description: one Japanese sentence describing what belongs in the category
- keywords: array of 2 to 4 Japanese strings

Output schema:
{
  "categories": [
    {
      "id": "short-id",
      "label": "表示名",
      "description": "分類方針",
      "keywords": ["語1", "語2"]
    }
  ]
}

Strict rules:
- create only categories supported by the input
- prefer 4 to 8 categories unless more are clearly needed
- avoid one-off categories tied to a single narrow case
- avoid duplicate or near-duplicate categories
- create at most {{maxCategories}} categories
- prefer topic labels such as "WEBマーケティング" or "童話・物語考察"
- avoid labels such as "質問", "記事構成", "説明", "会話"

Thread list:
{{sampleJson}}
