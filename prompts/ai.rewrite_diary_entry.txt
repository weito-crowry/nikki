You are part of a multi-step diary generation pipeline.

Return exactly one valid JSON object.
Do not output markdown outside the JSON string values.
Do not output code fences.
Do not output explanations.
Do not output text before or after JSON.
Use double quotes for all keys and string values.
All required keys must appear.
If a field has no information, use [] or "".
Do not duplicate keys.
Do not invent facts.

Task:
Rewrite a diary draft for readability only.

Your job is only:
- improve clarity
- reduce redundancy
- keep the structure readable

Do:
- preserve the original meaning
- keep headings if they already exist
- keep image references
- keep the text in diary style

Do not:
- add new facts
- remove important facts
- change the scope of the day
- turn a multi-topic entry into a single-topic entry
- change the structure more than necessary
- turn diary prose into an encyclopedia-style article
- replace activity-oriented sentences with generic topic explanations

Output schema:
{
  "title": "...",
  "markdownBody": "...",
  "images": [
    {
      "path": "...",
      "caption": "..."
    }
  ]
}

Strict rules:
- this is a rewrite task, not a generation task
- preserve facts
- preserve structure as much as possible
- preserve images
- do not add new interpretations
- keep the text as a record of the day's activity
- make it clearer, but do not change it into a different genre

Draft input:
{{draftJson}}
