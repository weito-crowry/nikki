You are part of a multi-step diary generation pipeline.

Return exactly one valid JSON object.
Do not output markdown.
Do not output code fences.
Do not output explanations.
Do not output text before or after JSON.
Use double quotes for all keys and string values.
All required keys must appear.
If a field has no information, use [] or "".
Do not duplicate keys.
Do not invent facts.

Task:
Write a diary draft for one day from structured summaries.

Your job is only:
- create a draft title
- create a lead
- create sections
- create a closing
- carry image references forward

Do:
- cover the whole day
- include multiple topics when they exist
- organize different topics into separate sections
- keep the text readable and concrete
- write from the perspective of a daily record of what was explored, asked, checked, or produced

Do not:
- ignore input summaries
- collapse everything into one topic if multiple topics exist
- invent events that are not in the input
- over-polish the style
- turn the entry into a neutral encyclopedia article
- spend most of the text explaining the subject itself instead of recording the day's activity

Output schema:
{
  "title": "...",
  "lead": "...",
  "sections": [
    {
      "heading": "...",
      "body": "..."
    }
  ],
  "closing": "...",
  "images": [
    {
      "path": "...",
      "caption": "..."
    }
  ]
}

Strict rules:
- title must represent the whole day, not one narrow topic
- if multiple topics exist, use multiple sections
- lead should describe the overall day briefly
- closing should summarize the day briefly
- preserve factual accuracy
- do not add facts that are not in the input
- each section should say what was handled or learned that day about the topic
- prefer "今日は〜について確認した / 整理した / 作成した" style over generic explanatory prose
- if the input is mostly one thread, still write it as a diary record of the activity, not as a standalone article

Entry id: {{entryId}}
Entry date: {{entryDate}}

Entry input:
{{entryJson}}
